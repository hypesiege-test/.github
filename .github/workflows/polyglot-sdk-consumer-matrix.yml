name: Polyglot SDK consumer matrix

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: '53 3 * * 2,5'
  workflow_dispatch:
  repository_dispatch:
    types: [clients-updated]

permissions:
  contents: read

jobs:
  sdk-clients:
    uses: zed-pkg-test/.github/.github/workflows/sdk-client-language-matrix.yml@df3eed96202810b7e7d6667fabc3041b19e6a22f
    with:
      clients_repository: hypesiege/hypesiege-clients
      clients_ref: main
      clients_root: clients
      interfaces_repository: hypesiege/hypesiege-interfaces
      interfaces_ref: main
      interfaces_path: hypesiege-interfaces
      interfaces_dependency: hypesiege/hypesiege-interfaces
    secrets:
      read_token: ${{ secrets.TEST_ORG_READ_TOKEN }}
